enemy.armor.head.hp;

state.entities[0].hp;

ptr.*.deref;

node.(*Player).position.x;

call.to().result;

player.hp -= enemy.attack_power * enemy.level;

if name.count == 0 || false {

}

if name. == 0 || false {

}

if dt.expression.kind.count >= 0 {

}
